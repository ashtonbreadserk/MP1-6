#include ‹iostream›
#include ‹string› 
using namespace std;
int main() { 
double length, width, area, perimeter;
cout << "Length: ";
cin >> Length;
cout << "width: ";
 cin >> width;
area = (length * width);
perimeter = 2 * (length + width);
cout << "Area:" << area << endl;
cout << "Perimeter:" << perimeter << endl;
return 0;
}
